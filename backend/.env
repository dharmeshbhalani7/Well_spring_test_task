DATABASE_URL=postgresql://username:password@localhost:5434/wellspring
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=24h
AWS_REGION=us-east-1
 
# for local testing purpose using floci.io instead of actual AWS S3 bucket and if you want to use actual AWS S3 bucket then remove this and add the actual AWS S3 bucket credentials
AWS_ENDPOINT_URL=http://localhost:4566

AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
S3_BUCKET=wellspring-media-dev
S3_PRESIGN_EXPIRES_SECONDS=900
PORT=3001
LOG_LEVEL=info
APP_URL=http://localhost:3000
NODE_ENV=development
